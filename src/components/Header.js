import React from 'react';
import Logo from './Header/Logo';
import "./Header.css";

const Header = () => {
    return (
        <header>
            <Logo />
        </header>
    );
}

export default Header;
